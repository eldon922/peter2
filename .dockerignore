# Version control
.git
.gitignore

# Dependencies (rebuilt inside Docker)
node_modules

# Next.js build output (built inside Docker)
.next
out

# Local env files — secrets are injected at runtime via --env-file
.env*
!.env.local.example
!.env.example

# Dev / editor artefacts
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
coverage

# CI/CD & GitHub
.github

# TypeScript build info
*.tsbuildinfo
next-env.d.ts
